 /*

#include <iostream>

using namespace std;

int index[1001], index_size = 0, answer = 0;

int dfs(int now, int num, int size, bool changed)
{

}

int main()
{	
	cin >> index_size;
	for (int i = 0; i < index_size; i++)
		cin >> index[i];

	dfs(0, index[0], 1, false);
}

*/